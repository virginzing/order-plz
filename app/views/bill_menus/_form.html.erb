<%= form_with(model: bill_menu, local: true) do |form| %>
  <% if bill_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bill_menu.errors.count, "error") %> prohibited this bill_menu from being saved:</h2>

      <ul>
      <% bill_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :menu_id %>
    <%= form.text_field :menu_id %>
  </div>

  <div class="field">
    <%= form.label :bill_id %>
    <%= form.text_field :bill_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
